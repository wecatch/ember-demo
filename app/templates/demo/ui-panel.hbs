{{outlet}}

<div class="ui container">
	<h4>普通面板</h4>
	{{#ui-tab-menu tabs='first,second' }}
		
	{{/ui-tab-menu}}
	{{#ui-tab-segment tab='first' active=true}}
		first
	{{/ui-tab-segment}}
	{{#ui-tab-segment tab='second'}}
		second
	{{/ui-tab-segment}}
</div>