import { formatByte } from '../../../helpers/format-byte';
import { module, test } from 'qunit';

module('Unit | Helper | format byte');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatByte(42);
  assert.ok(result);
});
